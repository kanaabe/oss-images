//-
//- The list template that renders the acutal commits. This template
//- is used on the server via `include` in the index.jade file, and this same
//- template is used on the client via
//- [Jadeify](https://github.com/domenic/jadeify).
//-

for item in list
  a(href = "http://artsy.net/artwork/#{item.id}")
    if item._links.thumbnail
      img(src = item._links.thumbnail.href)
